#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]){
    int a, b;
    printf("Masukan Nilai 1 : "); scanf("%d",&a);
    printf("Masukan Nilai 2 : "); scanf("%d",&b);
    
    if(a>b)
    printf("Nilai A : %d Lebih besar dari nilai B : %d\n",a,b);
    else if (a<b)
    printf("Nilai A : %d Lebih kecil dari nilai B : %d\n",a,b):
    else
    printf("Nilai A : %d Sama dengan nilai B : %d\n",a,b):
                  
    
  system("PAUSE");	
  return 0;
}
