program project1;

uses crt;

begin

  // Parent
  window(2,2,80,40); textbackground(red);
  clrscr();

  // Mata Kiri
  // window(6,6,34,16); textbackground(white);
  // clrscr;
  // Mata Dalam Kiri
  // window(8,8,32,14); textbackground(black);
  // clrscr;

  // Nose
  // window(0,0,80,40); textbackground(green);

  // Mata Kanan
  // window(46,6,74,16); textbackground(white);
  // clrscr;
  // Mata Dalam Kanan
  // window(48,8,72,14); textbackground(black);
  // clrscr;

  // Mulut
  // A
  // window(20,18,60,24); textbackground(white);
  // clrscr();
  // B
  // window(20,65,60,70); textbackground(white);
  // clrscr;
  // C
  // window(20,65,60,70); textbackground(black);
  // clrscr;
  readln;

end.
